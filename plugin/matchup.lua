-- https://github.com/andymass/vim-matchup/

-- let matchup show offscreen paren matches overtop the top buffer line, instead of taking over the status bar
vim.g.matchup_matchparen_offscreen = { method = "popup" }
